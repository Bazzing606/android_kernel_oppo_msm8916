version: 2.1
jobs:
  compile:
   docker:
      - image: ubuntu:14.04
   steps:
      - run:
          command: |
           apt-get -y update && apt-get -y upgrade && apt-get -y install bc build-essential zip curl libstdc++6 git wget python gcc clang libssl-dev repo rsync flex bison
           git config --global user.name "Bazzing606"
           git config --global user.email "erzatp720@gmail.com"
           git config --global color.ui false
           git clone https://github.com/akhilnarang/scripts && cd scripts && bash setup/android_build_env.sh && cd ..
           mkdir Alive-Q
           cd Alive-Q
           wget https://github.com/Bazzing606/android_kernel_oppo_msm8916/lineage-17.1/.circleci/build.sh
           source build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
